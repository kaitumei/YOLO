zhlkv1.onnx能检测所有的类，对视频的识别不行，一个模型能识别两个框是因为数据集不够，识别不够精准

zhlkv2.onnx只是搞了视频识别，无法识别车牌事故什么的

zhlkv3.onnx是综合前两者来训练的，按理说都能识别，ONNX格式运行效率更高，兼容性更好

hyttk.onnx是针对车牌事故的，识别效果最好

# 说明：全部模型已从PT格式转换为ONNX格式
# ONNX格式的优势：
# 1. 跨平台兼容性更好
# 2. 运行效率更高
# 3. 内存占用更低
# 4. 支持更多推理后端

zhlkv1.onnx
zhlkv2.onnx
zhlkv3.onnx
hyttk.onnx